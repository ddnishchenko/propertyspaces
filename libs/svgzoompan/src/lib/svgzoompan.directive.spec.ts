import { SvgZoomPanDirective } from './svgzoompan.directive';

describe('SvgZoomPanDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgZoomPanDirective();
    expect(directive).toBeTruthy();
  });
});
