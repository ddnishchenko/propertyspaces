import { SubjxDirective } from './subjx.directive';

describe('SubjxDirective', () => {
  it('should create an instance', () => {
    const directive = new SubjxDirective();
    expect(directive).toBeTruthy();
  });
});
