import { GoogleStreetViewDirective } from './google-street-view.directive';

describe('GoogleStreetViewDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleStreetViewDirective();
    expect(directive).toBeTruthy();
  });
});
