import { InputFileReaderDirective } from './input-file-reader.directive';

describe('InputFileReaderDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFileReaderDirective();
    expect(directive).toBeTruthy();
  });
});
